// This a test file template
var
  should = require('should'),
  sinon = require('sinon');

describe('Test: testTemplate', function () {
  var
    sandbox;

  before(() => {
  });

  beforeEach(() => {
    sandbox = sinon.sandbox.create();
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('#testContext', function () {
    it('testItem', () => {
    });
  });
});
